# Minimal requirements for subnet_accurate_validator.py and validation stack
# Pinned to versions compatible with Python 3.10 and existing Torch 2.4.0 env

# Core
numpy==1.26.4
# Keep torch from conda env; ensure version is recent enough
# If torch is missing in your env, uncomment the next line and pick the right build for your CUDA
# torch==2.4.0

# Vision stack matching torch 2.4.x
torchvision==0.19.0
torchmetrics==1.8.1

# HF + Transformers
transformers==4.44.2
huggingface_hub>=0.23.0

# CLIP implementations
open_clip_torch==2.29.0
openai-clip==1.0.1

# Rendering / I/O
gsplat @ git+https://github.com/nerfstudio-project/gsplat.git@2323de5905d5e90e035f792fe65bad0fedd413e7
meshio==5.3.5
imageio==2.35.1
pillow>=10.0.0,<11.0.0
zstandard==0.23.0

# Validation server imports required at import-time by validation.serve
fastapi==0.111.0
uvicorn==0.30.1
pybase64==1.4.0

# Logging
loguru==0.7.3

# ML utils
scikit-learn==1.6.1
joblib>=1.3.0
pytorch_lightning==2.5.2

# Outlier detection used by validation (transitively pulled)
pyod==2.0.5
pytod==0.0.3

# TRELLIS/SPZ compression
pyspz @ git+https://github.com/404-Repo/spz.git@0.1

# Networking
requests>=2.32.0

# Tooling for building native deps
ninja==1.11.1.1

# Workaround for packages importing `packaging` from `pkg_resources`
setuptools==65.5.1
packaging>=24.0 